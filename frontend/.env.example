# Backend API URL (no trailing slash on /api/v1 — it is included below)
# Local dev:
VITE_API_URL=http://localhost:8000/api/v1

# Production example (Render):
# VITE_API_URL=https://your-backend-name.onrender.com/api/v1
